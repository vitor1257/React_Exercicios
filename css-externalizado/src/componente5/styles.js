import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    padding: 30,
    marginTop: 40,
  },
  imagem: {
    width: 150,
    height: 150,
  },
});

export {styles}